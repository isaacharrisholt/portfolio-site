runtime: python310

service: db

entrypoint: gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app

env_variables:
  SERVICE_MODE: prod
  USE_TRUSTED_HOSTS: true
  API_HOST: api.example.com
  DATABASE_URL: cockroachdb://db.example.com:26257/example